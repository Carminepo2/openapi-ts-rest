{
  "name": "openapi-ts-rest-contract",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --parallel",
    "lint": "turbo lint",
    "test": "turbo test",
    "deploy": "turbo deploy",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@eslint/js": "^9.3.0",
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@vitest/coverage-istanbul": "^1.6.0",
    "eslint": "^9.3.0",
    "typescript-eslint": "^7.9.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
